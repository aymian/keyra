<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title + ' | Keyra' : 'Keyra - Secure Login' %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        slate: {
                            800: '#1e293b',
                            900: '#0f172a',
                        },
                        teal: {
                            600: '#0d9488',
                            700: '#0f766e',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
        }
    </style>
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>

<body class="bg-slate-50 text-slate-900 min-h-screen flex flex-col">
    <header class="w-full bg-[#1e293b] text-white py-4 px-6 shadow-md">
        <div class="max-w-7xl mx-auto flex justify-between items-center">
            <a href="/" class="flex items-center gap-2 group">
                <div
                    class="w-8 h-8 rounded bg-teal-600 flex items-center justify-center text-white font-bold text-xl shadow-lg group-hover:bg-teal-500 transition-colors">
                    K
                </div>
                <span class="text-xl font-bold tracking-tight">Keyra</span>
            </a>
            <nav class="flex items-center gap-6">
                <a href="/developers"
                    class="text-slate-300 hover:text-white transition-colors text-sm font-medium">Developers</a>
                <% if (typeof user !=='undefined' && user) { %>
                    <div class="flex items-center gap-4">
                        <span class="text-slate-300 text-sm hidden md:block">
                            <%= user.email %>
                        </span>
                        <form action="/auth/logout" method="POST" class="inline">
                            <button type="submit"
                                class="text-sm font-medium text-white bg-slate-700 hover:bg-slate-600 px-4 py-2 rounded-md transition-colors">
                                Logout
                            </button>
                        </form>
                    </div>
                    <% } else { %>
                        <a href="/auth/login"
                            class="text-sm font-medium text-teal-400 hover:text-teal-300 transition-colors">Login</a>
                        <a href="/auth/signup"
                            class="text-sm font-medium bg-teal-600 hover:bg-teal-500 text-white px-4 py-2 rounded-md shadow-lg transition-all hover:shadow-teal-500/20">
                            Sign Up
                        </a>
                        <% } %>
            </nav>
        </div>
    </header>

    <main class="flex-grow flex flex-col items-center justify-center p-4">